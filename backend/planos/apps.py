"""Configuração do aplicativo Planos."""

from django.apps import AppConfig
class PlanosConfig(AppConfig):
    """Configuração para o app 'planos'."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'planos'
